import { SIGN_UP_MODAL, SIGN_IN_MODAL } from "../constants";

export const displaySignUpModal = () => {
  return { type: SIGN_UP_MODAL };
};
export const displaySignInModal = () => {
  return { type: SIGN_IN_MODAL };
};
